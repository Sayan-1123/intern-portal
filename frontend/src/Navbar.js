import React from 'react';
import './Navbar.css';

const Navbar = () => (
  <nav className="navbar">
    <div className="app-title">Intern Portal</div>
    <button className="logout-btn">Logout</button>
  </nav>
);

export default Navbar;
